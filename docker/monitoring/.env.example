# Grafana bootstrap administrator account
#
# Copy this file to .env before the first deployment:
#   cp .env.example .env
#
# Replace the password with a strong unique value.
# The real .env file is excluded from Git.

GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=replace-with-a-strong-unique-password
